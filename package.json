{
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "start": "swc-node src/index.ts",
    "start:watch": "nodemon --ext ts --exec npm start",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@swc/core": "^1.2.74",
    "@swc/jest": "^0.1.4",
    "@swc/register": "^0.1.7",
    "@types/jest": "^26.0.24",
    "jest": "^27.0.6",
    "nodemon": "^2.0.12",
    "prettier": "^2.3.2"
  }
}
